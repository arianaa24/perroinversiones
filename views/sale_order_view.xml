<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="perroinversiones_rental_order_primary_form_view" model="ir.ui.view">
        <field name="name">perroinversiones.rental.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_renting.rental_order_primary_form_view"/>
        <field name="arch" type="xml">
            <button name="action_cancel" position="after">
                <button name="action_replicar" type="object" string="Replicar"/>
            </button>
        </field>
    </record>
</odoo>